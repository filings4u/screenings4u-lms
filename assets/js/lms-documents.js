(()=>{const $=x=>document.getElementById(x);let session,statusData;async function call(body){const r=await fetch(window.SCREENINGS4U_SUPABASE_URL+'/functions/v1/lms-learner-documents',{method:'POST',headers:{'Content-Type':'application/json','apikey':window.SCREENINGS4U_SUPABASE_ANON_KEY,'Authorization':'Bearer '+session.access_token},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw Error(d.error||'Request failed');return d}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}async function load(){const d=await call({action:'list'}),rows=[];if(d.consent)rows.push(`<tr><td><b>Learning Center Acknowledgment</b></td><td>Onboarding / Terms</td><td>Accepted</td><td>${new Date(d.consent.accepted_at).toLocaleDateString()}</td><td><button class="orange-btn" id="consentPdf">Download PDF</button></td></tr>`);for(const x of d.documents){const doc=Array.isArray(x.documents)?x.documents[0]:x.documents;rows.push(`<tr><td><b>${esc(doc?.title||'Document')}</b></td><td>${esc(x.category.replaceAll('_',' '))}</td><td>${esc(x.status)}</td><td>${new Date(x.created_at).toLocaleDateString()}</td><td><button class="orange-btn" data-doc="${x.id}">Download</button></td></tr>`)}$('docsBody').innerHTML=rows.join('')||'<tr><td colspan="5">No documents yet.</td></tr>';document.querySelectorAll('[data-doc]').forEach(b=>b.onclick=async()=>{const d=await call({action:'signed',id:b.dataset.doc});location.href=d.url});if($('consentPdf'))$('consentPdf').onclick=()=>pdf(d.consent)}function pdf(c){const {jsPDF}=window.jspdf,doc=new jsPDF();doc.setFont('helvetica','bold');doc.setFontSize(20);doc.text('screenings4u | Learning Center',20,22);doc.setFontSize(15);doc.text('Learning Center Acknowledgment',20,34);doc.setFont('helvetica','normal');doc.setFontSize(10);const lines=[`Name: ${c.first_name} ${c.last_name}`,`Email: ${c.email}`,`Phone: ${c.phone||'—'}`,`Accepted: ${new Date(c.accepted_at).toLocaleString()}`,`Consent version: ${c.consent_version}`,'','Acknowledgments:','• Learning Center Terms of Use accepted','• Refund Policy acknowledged: online courses and digital products are final and non-refundable once purchased.','• Training Disclaimer acknowledged','• DOT mock-training requirement acknowledged: documentation for five required mock collection sessions must be submitted for applicable certificate processing.','','Legal effective date: January 1, 2026','Legal updated date: August 23, 2026'];let y=46;for(const l of lines){for(const z of doc.splitTextToSize(l,170)){doc.text(z,20,y);y+=6}}doc.save('screenings4u-learning-center-acknowledgment.pdf')}document.addEventListener('DOMContentLoaded',async()=>{const db=await window.getScreenings4uSupabase();session=(await db.auth.getSession()).data.session;if(!session)return;statusData=await call({action:'status'});$('enrollment').innerHTML='<option value="">Select your DOT course</option>'+statusData.enrollments.map(e=>{const c=Array.isArray(e.lms_courses)?e.lms_courses[0]:e.lms_courses;return `<option value="${e.id}">${esc(c?.title||'Course')}</option>`}).join('');$('uploadBtn').onclick=async()=>{const f=$('mockFile').files[0];if(!f||!$('enrollment').value)return alert('Choose your course and a document.');const r=new FileReader;r.onload=async()=>{try{$('uploadBtn').disabled=true;await call({action:'upload_mock',enrollmentId:$('enrollment').value,file:{name:f.name,mimeType:f.type,base64:String(r.result)}});$('uploadStatus').hidden=false;$('uploadStatus').textContent='Uploaded successfully. screenings4u has been notified and your document is awaiting review.';$('mockFile').value='';await load()}catch(e){alert(e.message)}finally{$('uploadBtn').disabled=false}};r.readAsDataURL(f)};await load()})})();